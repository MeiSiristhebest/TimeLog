# Workflow Status File
# Generated by BMad Method workflow-init
# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-09"
project: "TimeLog"
project_type: "mobile-app"
selected_track: "method"
field_type: "greenfield"
workflow_path: "_bmad/bmm/workflows/workflow-status/paths/method-greenfield.yaml"

workflow_status:
  # ============================================
  # PHASE 1: Analysis (Optional Discovery)
  # ============================================
  phase_1_analysis:
    name: "Analysis (Optional)"
    optional: true
    workflows:
      brainstorm-project:
        status: "skipped"
        agent: "analyst"
        command: "/bmad-bmm-workflows-brainstorming"
        note: "User did not select"

      research:
        status: "_bmad-output/planning-artifacts/research/technical-timelog-voice-pipeline-research-2026-01-09.md"
        agent: "analyst"
        command: "/bmad-bmm-workflows-research"
        note: "Complete - Technical research on LiveKit + Deepgram + Gemini integration"

      product-brief:
        status: "_bmad-output/planning-artifacts/product-brief-TimeLog-2026-01-09.md"
        agent: "analyst"
        command: "/bmad-bmm-workflows-create-product-brief"
        note: "Complete - Product Brief created from TimeLog Proposal"

  # ============================================
  # PHASE 2: Planning
  # ============================================
  phase_2_planning:
    name: "Planning"
    required: true
    workflows:
      prd:
        status: 'complete'
        agent: "pm"
        command: "/bmad-bmm-workflows-create-prd"
        output_file: '_bmad-output/planning-artifacts/prd.md'
        next_workflow: 'create-ux-design'

      create-ux-design:
        status: "complete"
        condition: "if_has_ui"
        agent: "ux-designer"
        command: "/bmad-bmm-workflows-create-ux-design"
        output_file: "_bmad-output/planning-artifacts/ux-design-specification.md"
        note: "Complete - UX Design Specification for TimeLog"

  # ============================================
  # PHASE 3: Solutioning
  # ============================================
  phase_3_solutioning:
    name: "Solutioning"
    required: true
    workflows:
      create-architecture:
        status: "complete"
        agent: "architect"
        command: "/bmad-bmm-workflows-create-architecture"
        output_file: "_bmad-output/planning-artifacts/architecture.md"

      create-epics-and-stories:
        status: "complete"
        agent: "pm"
        command: "/bmad-bmm-workflows-create-epics-and-stories"
        output_file: "_bmad-output/planning-artifacts/epics.md"

      test-design:
        status: "optional"
        agent: "tea"
        command: "/bmad-bmm-workflows-testarch-test-design"
        output: "System-level testability review"

      implementation-readiness:
        status: "_bmad-output/planning-artifacts/implementation-readiness-report-2026-01-10.md"
        agent: "architect"
        command: "/bmad-bmm-workflows-check-implementation-readiness"
        note: "Gate check complete; see implementation-readiness-report-2026-01-10.md"

  # ============================================
  # PHASE 4: Implementation
  # ============================================
  phase_4_implementation:
    name: "Implementation"
    required: true
    workflows:
      sprint-planning:
        status: "in-progress"
        agent: "sm"
        command: "/bmad-bmm-workflows-sprint-planning"
        note: "Sprint plan created; execution tracked in _bmad-output/implementation-artifacts/sprint-status.yaml"

# ============================================
# NEXT ACTION
# ============================================
next_workflow:
  id: "sprint-planning"
  name: "Sprint Planning"
  agent: "sm"
  command: "/bmad-bmm-workflows-sprint-planning"
  reason: "Implementation readiness completed (2026-01-10 report). Continue executing against sprint-status.yaml."
